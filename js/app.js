/*! * media-assembly-kit-simple-template - v0.1.0 * * https://www.digitalcube.jp/ * * Copyright 2014, DigitalCube Co.,Ltd (https://www.digitalcube.jp/) * Released under the GNU General Public License v2 or later */(function ($) {	/* search toggle */	var searchBtn  = $('#search-box-toggle'),	    searchForm = $('#search-box .search-form');	searchBtn.on('click', function () {		searchForm.slideToggle( function() {			// Animation complete.			searchBtn.children('.fa').toggleClass('fa-search');			searchBtn.children('.fa').toggleClass('fa-times');			searchForm.toggleClass('open');		});	});	/* menu toggle */	var menuBtn   = $('#site-navigation-toggle'),	    menu      = $('#site-navigation-menu'),	    body      = $('#page'),	    menuWidth = menu.outerWidth();	menuBtn.on('click', function () {		body.toggleClass('open');		menuBtn.children('.fa').toggleClass('fa-bars');		menuBtn.children('.fa').toggleClass('fa-times');		if ( body.hasClass('open') ) {			body.animate( {'left': menuWidth}, 300 );			menu.animate( {'left': 0}, 300 );		} else {			menu.animate( {'left': -menuWidth}, 300 );			body.animate( {'left': 0}, 300 );		}	});})(jQuery);